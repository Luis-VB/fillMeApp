<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- actionColorBar Theme -->
    <style name="MainActivityTheme" parent="FillMeApp">
        <item name="colorPrimary">@color/actionBarColor</item>
    </style>

    <!-- Base application theme. -->
    <style name="FillMeApp" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:background">@drawable/rounded_button</item>
        <item name="android:textColor">@color/buttonText</item>
        <item name="android:padding">10dp</item>
        <item name="android:layout_margin">5dp</item>
    </style>

    <style name="GlobalTextStyle" parent="TextAppearance.AppCompat">
        <item name="android:textColor">@color/TextPrimary</item>
        <item name="android:textSize">20sp</item>
    </style>

    <!-- Your new theme. -->
    <style name="NewTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/newColorPrimary</item>
        <item name="colorPrimaryDark">@color/newColorPrimaryDark</item>
        <item name="colorAccent">@color/newColorAccent</item>

    </style>
</resources>